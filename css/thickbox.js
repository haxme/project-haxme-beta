/* Compatibility stub for legacy pages that still include Thickbox.
 * The restored app does not ship Thickbox's original script, and no restored
 * page calls its runtime API. Keeping this file prevents 404s without changing
 * those historical templates.
 */
